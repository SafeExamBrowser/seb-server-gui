<template>
    <!-- This essnetially eats a couple of parameters, like wether the current status is active or not, what the type of object is (certificate, assessment tool
    or connection config, and then a constant. It will then call up something like useDeactivate / useActivate (which will eat the constant that will be actually a url constant to build the url)-->
    <h1>activity dialog</h1>
</template>
