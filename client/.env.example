VITE_DEV_API_SERVER_TARGET=http://localhost:3001

# Only needed for `npm run openapi:refresh` / `npm run openapi:fetch:seb-server`,
# never for running or building the app.
SEB_SERVER_OPENAPI_URL=http://localhost:8080/v3/api-docs
